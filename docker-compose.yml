services:
  polyquant:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - polyquant-data:/data
    restart: unless-stopped
volumes:
  polyquant-data:
